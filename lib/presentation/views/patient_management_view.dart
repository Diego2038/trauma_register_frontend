import 'package:flutter/material.dart';

class PatientManagementView extends StatelessWidget {
  const PatientManagementView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Patient Management view");
  }
}
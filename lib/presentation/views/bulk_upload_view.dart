import 'package:flutter/material.dart';

class BulkUploadView extends StatelessWidget {
  const BulkUploadView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Bulk upload view");
  }
}